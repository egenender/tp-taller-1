#ifndef CAMARA_H_
#define CAMARA_H_

#include "Aplicacion.h"
#include "Observador.h"
#include "Posicion.h"

/*class Camara : public Observador {
	private:
		// Las dimensiones de la camara:
		SDL_Rect* camara;

	public:
		Camara();
		Camara(int x, int y);
		void actualizar(Observable* observable);
		int obtenerX();
		int obtenerY();
		Posicion* obtenerPosicion();
		SDL_Rect* obtenerDimensiones();
		~Camara();
};*/

#endif
